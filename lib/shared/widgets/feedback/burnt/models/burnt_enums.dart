part of '../burnt_library.dart';

/// Defines the visual preset for the toast, influencing the icon.
enum BurntPreset { done, warning, error, none }

/// Defines the duration for which the toast will be visible.
enum BurntDuration { short, long, infinite }

/// Defines the haptic feedback type to be played when the toast appears.
enum BurntHaptic { success, warning, error, none }
